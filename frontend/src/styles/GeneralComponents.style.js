import styled from "@emotion/styled";
import { Alert } from "@mui/material";

export const AlertComponent = styled(Alert, {})`
  width: 13.5rem;
`;

export const LoggedOrCreatedSuccessfullyDiv = styled.div`
  display: flex;
  justify-content: center;
  flex-direction: column;
  align-items: center;
  margin-top: 7rem;
`;

export const LoginOrCreateTitle = styled.h2`
  padding: 0 2rem;
  margin-top: 7rem;
  margin-inline: 3.5rem;
`;

export const LoginOrCreateTitleDiv = styled.div`
  display: flex;
  justify-content: center;
  margin-top: 5rem;
`;

export const LoginOrCreateButtonDiv = styled.div`
  display: flex;
  justify-content: center;
`;
